---
phase: 02-playable-core-loop
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/harness/PlayHarness.tsx
  - src/harness/ResultScreen.tsx
  - src/harness/GameHarness.tsx
  - src/harness/PlayHarness.test.tsx
  - src/harness/ResultScreen.test.tsx
autonomous: true
requirements: [LOOP-01, LOOP-02, LOOP-03, LOOP-04, LOOP-05, LOOP-07, LOOP-08, LOOP-09, LOOP-10]
user_setup: []

must_haves:
  truths:
    - "The teacher presses 카드 뽑기, sees the mission name/desc/difficulty large, judges 성공/실패, and on 성공 rolls the dice to advance (LOOP-01..05)"
    - "The landed-square event (앞으로/뒤로/한 번 더) is shown and applied; 한 번 더 gives the same player another turn, else the next player goes (LOOP-07/08)"
    - "Reaching or passing the finish shows the result screen with the winner (LOOP-09); a co-winner tie lets the teacher pick (D-05)"
    - "A wall-clock countdown (default 20 min) and a 지금 순위로 마치기 button end the game with the furthest-along player winning (D-04)"
    - "From the result screen the teacher can 다시 시작 or return to the 시작 화면 (LOOP-10)"
  artifacts:
    - src/harness/PlayHarness.tsx
    - src/harness/ResultScreen.tsx
  key_links:
    - "the countdown timer lives ONLY in PlayHarness (DOM-owned); on 0 or manual it calls useGameStore.end(reason) — the engine never reads the clock (D-04, Pitfall 1)"
    - "GameHarness routes phase==='gameOver' → ResultScreen, otherwise → PlayHarness"
    - "다시 시작 re-runs createGame via the bridge; 시작 화면으로 resets game to null (LOOP-10)"
---

<objective>
Complete the playable loop: the plain-DOM play screen that drives the full turn (draw → judge → roll → move → event → advance), the wall-clock countdown + manual-end that resolves a session by furthest-along leader, and the result screen with winner / co-winner handling and restart. After this plan a real user can play a whole game from setup to victory (or timed finish) end to end.

Purpose: Land the remaining vertical slices (LOOP-01..05, 07..10) on the engine and the setup slice from 02-02, proving the entire core loop works before 3D (D-06, D-09). The timer stays DOM-owned so the engine remains pure and deterministic (D-04, RESEARCH Pitfall 1).
Output: `src/harness/{PlayHarness,ResultScreen}.tsx`, GameHarness routing to them, and component tests covering the phase-driven loop, timeout/manual end, and restart.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-playable-core-loop/02-RESEARCH.md
@.planning/phases/02-playable-core-loop/02-CONTEXT.md
@src/engine/types.ts
@src/engine/engine.ts
@src/harness/useGameStore.ts
@src/harness/GameHarness.tsx
@src/schema.ts
</context>

<artifacts>
## Artifacts this plan produces

**src/harness/PlayHarness.tsx** — phase-driven play UI + DOM countdown:
- awaitingDraw → 카드 뽑기 button → useGameStore.draw() (LOOP-01)
- awaitingJudgement → large mission name/desc/difficulty + 성공/실패 → judge(true/false) (LOOP-02/03)
- awaitingRoll → 주사위 굴리기 → roll() (LOOP-05)
- turnResolved → shows lastRoll + lastLanding (from→to, label, 한 번 더) → 다음 → next() (LOOP-04/07/08)
- current participant/member banner (team member per memberTurnIndex, D-01) + all positions
- countdown timer effect (default 20 min) + 지금 순위로 마치기 button → end('timeout'|'manual') (D-04)

**src/harness/ResultScreen.tsx** — winner / co-winner (D-05) display + 다시 시작 (new game, same config) + 시작 화면으로 (reset) (LOOP-09/10)

**src/harness/GameHarness.tsx (edit)** — replace the 02-02 placeholder branch: phase==='gameOver' → ResultScreen, else → PlayHarness
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlayHarness — full turn loop UI + DOM countdown + manual end (LOOP-01..08, D-04)</name>
  <files>src/harness/PlayHarness.tsx, src/harness/PlayHarness.test.tsx</files>
  <read_first>
    - src/engine/types.ts (GameState.phase, card, lastRoll, lastLanding, config.participants — the fields this screen renders)
    - src/harness/useGameStore.ts (draw/judge/roll/next/end/setRemainingMs actions from 02-02)
    - src/engine/engine.ts (how phases advance — so the UI shows the right control per phase)
    - src/schema.ts (Mission name/desc/diff for LOOP-02; Effect + label for LOOP-07)
    - .planning/phases/02-playable-core-loop/02-CONTEXT.md §decisions D-04 (timer + 지금 순위로 마치기), D-06 (loop order), §Claude's Discretion (countdown display, team member-order banner)
    - .planning/phases/02-playable-core-loop/02-RESEARCH.md §Code Examples (harness timer effect), §Pitfall 1 (wall-clock is DOM-owned)
  </read_first>
  <behavior>
    - phase 'awaitingDraw' renders 카드 뽑기; clicking it calls useGameStore.draw() (LOOP-01)
    - phase 'awaitingJudgement' renders the drawn mission's name, desc, and difficulty prominently plus 성공 and 실패 buttons; 성공→judge(true), 실패→judge(false) (LOOP-02/03/04)
    - phase 'awaitingRoll' renders 주사위 굴리기; clicking calls useGameStore.roll() (LOOP-05)
    - phase 'turnResolved' shows lastRoll and the landing (from→to, event label, and a 한 번 더 marker when extraTurn) plus a 다음 button calling next() (LOOP-07/08)
    - a current-turn banner shows the active participant, and for a team the active member per memberTurnIndex (D-01); all participant positions are listed
    - when config.timeLimitMs is set, a countdown ticks down and calls end('timeout') at 0; a 지금 순위로 마치기 button calls end('manual') (D-04)
    - the countdown uses Date.now/setInterval INSIDE the component only — never in the engine (Pitfall 1)
  </behavior>
  <action>
    Create src/harness/PlayHarness.tsx reading `useGameStore((s) => s.game)` (non-null in this branch). Render a phase-driven control set following the exact loop order from D-06 — 카드 뽑기 (LOOP-01), then the large mission card (name/desc/diff, LOOP-02) with 성공/실패 (LOOP-03; 실패 advances with no move per LOOP-04), then 주사위 굴리기 (LOOP-05), then a turnResolved summary of the roll and landing (LOOP-07: forward/backward/extra via lastLanding.eff + label; LOOP-08: show 한 번 더 when extraTurn) with a 다음 button that calls next(). Show a turn banner for the current participant and, in team mode, which member takes this turn (memberTurnIndex, D-01); list every participant's position.

    Add the countdown with a useEffect that, when `game.config.timeLimitMs` is non-null and phase is not gameOver, computes remaining time from a captured start timestamp via Date.now() on a setInterval (~250ms), pushes it to setRemainingMs for display, and on reaching 0 clears the interval and calls `useGameStore.getState().end('timeout')`. Provide a 지금 순위로 마치기 button calling `end('manual')`. All wall-clock logic stays in this component (D-04, RESEARCH §Code Examples / Pitfall 1); the engine's endGame only receives the reason.

    Create PlayHarness.test.tsx (testing-library + jsdom). Drive deterministically by setting useGameStore.game to crafted GameStates (construct via the engine with a seeded mulberry32, or set states directly) for each phase and assert the correct control renders and the correct bridge action fires on click. Use vi.useFakeTimers to advance the countdown past the limit and assert end('timeout') runs; assert the manual button calls end('manual').
  </action>
  <acceptance_criteria>
    - `npm test -- src/harness/PlayHarness.test.tsx` passes
    - Each phase (awaitingDraw/awaitingJudgement/awaitingRoll/turnResolved) renders its expected control and the mission name/desc/diff is shown at awaitingJudgement (LOOP-01/02/03/05)
    - extraTurn state renders a 한 번 더 marker (LOOP-08, asserted)
    - Fake-timer advance past timeLimitMs triggers end('timeout'); 지금 순위로 마치기 triggers end('manual') (D-04, asserted)
    - No wall-clock in the engine remains true: `grep -rnE "Date\.now|setInterval|performance\.now" src/engine` returns zero lines (Pitfall 1)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/harness/PlayHarness.test.tsx</automated>
  </verify>
  <done>The full turn loop (draw→judge→roll→move→event→advance) is playable via buttons, with a DOM-owned countdown and manual-end that resolve the session — the engine stays clock-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ResultScreen + GameHarness routing — winner/co-winner + restart (LOOP-09/10, D-05)</name>
  <files>src/harness/ResultScreen.tsx, src/harness/GameHarness.tsx, src/harness/ResultScreen.test.tsx</files>
  <read_first>
    - src/engine/types.ts (GameState.winners: string[], endReason, config.participants — for name lookup)
    - src/harness/useGameStore.ts (startGame for 다시 시작, reset for 시작 화면으로 — from 02-02)
    - src/harness/GameHarness.tsx (the 02-02 placeholder branch to replace)
    - src/harness/PlayHarness.tsx (Task 1 — the play branch GameHarness routes to)
    - .planning/phases/02-playable-core-loop/02-CONTEXT.md §decisions D-05 (co-winner tie → teacher picks), D-03/D-04 (why endReason may be reached-finish vs timeout/manual)
  </read_first>
  <behavior>
    - phase 'gameOver' with a single winner shows that participant as the winner (LOOP-09)
    - winners.length > 1 shows a co-winner state ("공동 승리") and lets the teacher select the final winner among the tied participants (D-05)
    - endReason is reflected (reached-finish vs timeout/manual) in the result copy
    - 다시 시작 starts a new game with the SAME config (bridge startGame) and 시작 화면으로 resets to setup (game=null) (LOOP-10)
    - GameHarness renders ResultScreen when phase==='gameOver', PlayHarness otherwise, SetupScreen when game is null
  </behavior>
  <action>
    Create src/harness/ResultScreen.tsx reading `useGameStore((s) => s.game)`. Map `game.winners` ids to participant names from `game.config.participants`. Single winner → show the winner prominently (LOOP-09). When `winners.length > 1`, render a 공동 승리 state listing the tied participants with a select control so the teacher chooses the final winner (D-05, Pitfall 2) — the selection is display-only harness state (no engine change needed). Reflect endReason ('reached-finish' vs 'timeout'/'manual') in the copy. Provide 다시 시작 → build the same config and call the bridge's startGame again (LOOP-10 restart), and 시작 화면으로 → call reset() to return to SetupScreen (LOOP-10).

    Edit src/harness/GameHarness.tsx to replace the 02-02 placeholder branch: when `game` is null render SetupScreen (unchanged), when `game.phase === 'gameOver'` render `<ResultScreen />`, otherwise render `<PlayHarness />`.

    Create ResultScreen.test.tsx: set useGameStore.game to a crafted gameOver state with a single winner → assert winner name shown and 다시 시작 / 시작 화면으로 present; set a gameOver state with two tied winners → assert 공동 승리 + a selection control (D-05); assert 시작 화면으로 calls reset (game → null).
  </action>
  <acceptance_criteria>
    - `npm test -- src/harness/ResultScreen.test.tsx` passes
    - Single-winner gameOver shows the winner and both restart controls (LOOP-09/10, asserted)
    - Tied winners render 공동 승리 with a teacher-selectable control (D-05, asserted)
    - 시작 화면으로 resets game to null (LOOP-10, asserted)
    - `npm test` full suite passes and `npm run build` succeeds with all harness screens wired
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/harness/ResultScreen.test.tsx</automated>
  </verify>
  <done>Winning (or timed finish) shows a result screen with single/co-winner handling and both restart paths; GameHarness routes setup/play/result — the whole loop is playable end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM timer → engine | The countdown/manual button crosses into the engine only as an end(reason) call; no clock value enters the engine (D-04, Pitfall 1) |
| harness state → render | Winner selection is display-only harness state; no new persistence or network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering | wall-clock in engine | medium | mitigate | Countdown is confined to PlayHarness; grep gate proves engine has no Date/setInterval (keeps engine deterministic + testable) |
| T-02-09 | Denial of Service | setInterval leak across games | low | mitigate | useEffect returns clearInterval; effect re-keys on phase and clears on gameOver (RESEARCH §Code Examples) |
| T-02-10 | Repudiation | none (no accounts) | low | accept | Single-device offline classroom tool; teacher-resolved co-winner needs no audit trail |

No new dependencies installed (RESEARCH §Package Legitimacy Audit) — no supply-chain checkpoint applies.
</threat_model>

<verification>
- `npm test` — full suite green (PlayHarness + ResultScreen tests added; engine + Phase 1 + 02-02 suites unchanged)
- `npm run build` — type-checks and builds with all three game screens wired
- No wall-clock in engine: `grep -rnE "Date\.now|setInterval|performance\.now" src/engine` returns nothing (D-04/Pitfall 1)
- Manual sanity (optional): `npm run dev`, play a full game start→win; test 지금 순위로 마치기 mid-game
</verification>

<success_criteria>
- A full game is playable end to end via buttons: draw → judge → roll → move → event → advance → win/result (LOOP-01..09, D-06)
- 한 번 더 repeats the same player's turn; other events pass to the next player (LOOP-08)
- The 20-min countdown or 지금 순위로 마치기 ends the session with the furthest-along leader winning; ties show co-winners for teacher resolution (D-04, D-05)
- The result screen offers 다시 시작 and 시작 화면으로 (LOOP-10); the engine stays clock-free
</success_criteria>

<output>
Create `.planning/phases/02-playable-core-loop/02-03-SUMMARY.md` when done. Record the play/result harness components, the DOM-timer approach (start timestamp + setInterval + end(reason)), how GameHarness routes setup/play/result, the co-winner resolution UX, and confirmation that a full game plays start→finish. Note that this harness is throwaway — Phase 3 replaces it with the 3D board + real child UI.
</output>
