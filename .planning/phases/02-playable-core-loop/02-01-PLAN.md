---
phase: 02-playable-core-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/types.ts
  - src/engine/rng.ts
  - src/engine/placement.ts
  - src/engine/setup.ts
  - src/engine/engine.ts
  - src/engine/rng.test.ts
  - src/engine/placement.test.ts
  - src/engine/setup.test.ts
  - src/engine/engine.test.ts
autonomous: true
requirements: [LOOP-01, LOOP-03, LOOP-04, LOOP-05, LOOP-07, LOOP-08, LOOP-09, EVENT-06, MISSION-07]
user_setup: []

must_haves:
  truths:
    - "A full solo game can be driven start→win in a unit test with zero React/DOM imports (D-09)"
    - "Reaching OR passing the finish square wins immediately; overshoot is allowed, exact landing is NOT required (D-03)"
    - "endGame(reason) returns co-winners when the furthest-along participants tie at the same square (D-04, D-05)"
    - "A team is ONE Participant sharing ONE token; members rotate turns via memberTurnIndex (D-01)"
    - "Dice rolls are ~uniform and event placement follows weight, proven by seeded distribution tests (D-07)"
    - "canStart(missions) blocks an empty mission library with guidance (MISSION-07, D-08)"
  artifacts:
    - src/engine/types.ts
    - src/engine/rng.ts
    - src/engine/placement.ts
    - src/engine/setup.ts
    - src/engine/engine.ts
  key_links:
    - "engine imports Mission/Event/Effect from src/schema.ts (Phase 1 contract) — NOT from src/store.ts"
    - "every random draw/roll/placement goes through an injected Rng (no global Math.random in transitions)"
    - "moveAndResolve never clamps the upper bound (overshoot win); backward clamps lower bound at 0 only"
---

<objective>
Build the pure, framework-agnostic headless game engine — the durable contract Phase 3 will reuse. This plan delivers the isolated seedable RNG module (with distribution tests), weighted event placement (EVENT-06), the setup/config builders (board presets, participant builder, empty-list guard MISSION-07), and the turn FSM (draw→judge→roll→move→event→win/timeout) with exhaustive unit tests.

Purpose: De-risk the whole project by proving the core game loop is correct and deterministic BEFORE any UI or 3D exists (D-09). This is the load-bearing module; the DOM harness (02-02/02-03) and the Phase 3 3D UI both consume its public API.
Output: `src/engine/{types,rng,placement,setup,engine}.ts` plus four vitest suites — all green, all pure (no React/DOM imports).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-playable-core-loop/02-RESEARCH.md
@.planning/phases/02-playable-core-loop/02-CONTEXT.md
@src/schema.ts
@src/lib/normalize.ts
@src/store.ts
</context>

<artifacts>
## Artifacts this plan produces (engine public API = Phase 3 contract)

**src/engine/types.ts**
- `Phase` = `'awaitingDraw' | 'awaitingJudgement' | 'awaitingRoll' | 'turnResolved' | 'gameOver'`
- `Participant { id, name, character: 'boy'|'girl', position: number, memberNames: string[], memberTurnIndex: number }`
- `GameConfig { mode: 'solo'|'team', participants: Participant[], boardLength: number, timeLimitMs: number|null }`
- `DrawnCard { mission: Mission }`
- `LandingResult { eventId: string|null, eff: Effect|null, label: string, from: number, to: number, extraTurn: boolean }`
- `EndReason` = `'reached-finish' | 'timeout' | 'manual'`
- `GameState { phase, config, boardEvents: (string|null)[], currentIndex, card, lastRoll, lastLanding, winners: string[], endReason }`

**src/engine/rng.ts** — `Rng { next(): number }`, `mulberry32(seed)`, `systemRng()`, `rollDie(rng)`, `weightedPick(items, weightOf, rng)`, `uniformPick(items, rng)`

**src/engine/placement.ts** — `placeEvents(events, boardLength, rng, density=0.5)`

**src/engine/setup.ts** — `BoardPreset`, `boardLengthFor(preset)`, `DEFAULT_TIME_LIMIT_MS`, `MAX_PARTICIPANTS`, `buildParticipants(...)`, `canStart(missions)`

**src/engine/engine.ts** — `createGame(config, missions, events, rng)`, `drawCard(state, missions, rng)`, `judge(state, success)`, `rollDice(state, events, rng)`, `advanceTurn(state)`, `endGame(state, reason)`, `resetGame(state, missions, events, rng)`
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Isolated seedable RNG module + engine types + distribution tests</name>
  <files>src/engine/rng.ts, src/engine/types.ts, src/engine/rng.test.ts</files>
  <read_first>
    - src/schema.ts (import Mission, Event, Effect — do NOT redefine them)
    - src/store.ts (note the top-of-file "PHASE 2 CONTRACT: MUST stay free of React/DOM imports" — mirror that discipline)
    - .planning/phases/02-playable-core-loop/02-RESEARCH.md §Pattern 2 (RNG injection), §Testing Strategy (distribution test shape, mulberry32 source [CITED])
  </read_first>
  <behavior>
    - rollDie over 60000 seeded rolls: each face 1..6 lands within ±5% of N/6
    - weightedPick with weights 3:2:1 over 60000 seeded picks: observed ratios within ±5% of 3/6, 2/6, 1/6
    - weightedPick on empty array → null; all-weights-zero → null; single positive weight → always that item
    - uniformPick on empty array → null; single item → always that item
    - rollDie output is always an integer in [1,6]
  </behavior>
  <action>
    Create src/engine/types.ts declaring the engine data model listed in the plan's Artifacts section. Import Mission, Event, Effect from '../schema' (Phase 1 contract) — declare NO new Zod schemas. Add short doc comments tying each Phase value to its LOOP req (awaitingDraw=LOOP-01, awaitingJudgement=LOOP-02/03, awaitingRoll=LOOP-05, turnResolved=LOOP-07/08, gameOver=LOOP-09). Participant.position is unbounded on the high side (overshoot allowed per D-03). GameState carries NO time field — the wall-clock timer is DOM-owned (D-04, RESEARCH Pitfall 1).

    Create src/engine/rng.ts: an `Rng` interface with `next(): number` returning [0,1). Implement `mulberry32(seed: number): Rng` as the inline public-domain PRNG from RESEARCH §Pattern 2 (cite the gist URL in a comment). Implement `systemRng(): Rng` preferring crypto.getRandomValues with a Math.random fallback (this is the only non-deterministic path; runtime only). Implement `rollDie(rng): number` = `1 + Math.floor(rng.next() * 6)` (1..6, LOOP-05). Implement `weightedPick<T>(items, weightOf, rng): T | null` using a cumulative sum; return null when items is empty OR total weight ≤ 0, with a final-item float-safety fallback (RESEARCH Pattern 2, Pitfall 3). Implement `uniformPick<T>(items, rng): T | null` = index `Math.floor(rng.next()*items.length)`, null on empty (used for equal-probability mission draw, LOOP-01 — missions carry no weight).

    These files must not import React, react-dom, or reference document/window (D-09).

    Create src/engine/rng.test.ts covering every case in the behavior block. Seed mulberry32 with a fixed constant so runs are deterministic (no flakiness).
  </action>
  <acceptance_criteria>
    - `npm test -- src/engine/rng.test.ts` passes (all behavior-block cases green)
    - Engine purity gate returns zero lines: `grep -rnE "^import .* from ['\"](react|react-dom)['\"]" src/engine`
    - rng.ts exports exactly: Rng, mulberry32, systemRng, rollDie, weightedPick, uniformPick
    - types.ts re-uses Mission/Event/Effect from ../schema (verified by import line present, no local `z.enum`/`z.object` for those)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/engine/rng.test.ts</automated>
  </verify>
  <done>Seeded dice + weighted/uniform picks are deterministic and provably fair; engine types compile and import Phase-1 schema types; no React/DOM imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Weighted event placement (EVENT-06) + setup/config builders + empty-list guard (MISSION-07)</name>
  <files>src/engine/placement.ts, src/engine/setup.ts, src/engine/placement.test.ts, src/engine/setup.test.ts</files>
  <read_first>
    - src/engine/rng.ts (from Task 1 — weightedPick signature)
    - src/engine/types.ts (from Task 1 — Participant, GameConfig, BoardPreset consumers)
    - src/schema.ts (Event.weight, Event.eff, Mission)
    - src/lib/normalize.ts (weight→% philosophy: total 0 handled as 0, no divide-by-zero — placement mirrors this safety)
    - .planning/phases/02-playable-core-loop/02-RESEARCH.md §Code Examples (placeEvents, setup builders), §Pitfall 3 (placement edge cases), §Pitfall 4 (team model)
  </read_first>
  <behavior>
    - placeEvents returns an array of length boardLength+1; index 0 (start) and index boardLength (finish) are always null
    - With events present and density=1, interior squares 1..boardLength-1 all carry a placed eventId; weight ratio of placed ids over many seeded boards tracks the weights (±5% band)
    - placeEvents with empty events → all-null board; placeEvents with all-zero-weight events → no eventId placed (all null)
    - boardLengthFor('short') === 20, boardLengthFor('normal') === 30 (Claude discretion A1, D-02 default short)
    - buildParticipants('solo', 3, names, chars) → 3 Participants, each position 0, memberNames=[own name], memberTurnIndex 0
    - buildParticipants('team', 2, teamNames, chars, memberNamesPerTeam) → 2 Participants (ONE token each), memberNames populated per team, memberTurnIndex 0 (D-01: shared token)
    - buildParticipants clamps count into [1, MAX_PARTICIPANTS] (input-bounds, threat T-02-01)
    - canStart([]) → { ok:false, reason:<guidance string> }; canStart([mission]) → { ok:true } (MISSION-07, D-08)
  </behavior>
  <action>
    Create src/engine/placement.ts exporting `placeEvents(events, boardLength, rng, density = 0.5)` per RESEARCH §Code Examples: allocate `new Array(boardLength+1).fill(null)`; return early all-null when events is empty; iterate interior squares 1..boardLength-1; skip a square when `rng.next() >= density` (leaves it empty); otherwise `weightedPick(events, e => e.weight, rng)` and place the picked id (null when the pick returns null — all-zero weights). Start (0) and finish (boardLength) stay null. density default 0.5 is Claude discretion per RESEARCH A2/EVENT-06 open question; document that distribution correctness is independent of density.

    Create src/engine/setup.ts exporting: `BoardPreset = 'short' | 'normal'`; `boardLengthFor(preset)` → short 20 / normal 30 (A1); `DEFAULT_TIME_LIMIT_MS = 20 * 60 * 1000` (D-04 default 20 min); `MAX_PARTICIPANTS = 8` and default count 2 (A3); `buildParticipants(mode, count, names, characters, memberNamesPerParticipant?)` returning Participant[] — solo gives each participant its own single-member token, team gives each team ONE token with its memberNames array (D-01, Pitfall 4); clamp count into [1, MAX_PARTICIPANTS]; assign ids via a stable local counter (do not import uid from seed to keep engine dependency-free of content). `canStart(missions)` → returns not-ok with a child-friendly Korean guidance string when missions is empty, ok otherwise (MISSION-07, D-08).

    All four files stay pure (no React/DOM). placement.ts and setup.ts import only from ./rng, ./types, and ../schema.

    Create placement.test.ts and setup.test.ts covering every behavior-block case with seeded mulberry32.
  </action>
  <acceptance_criteria>
    - `npm test -- src/engine/placement.test.ts src/engine/setup.test.ts` passes
    - Placement never assigns an eventId to index 0 or index boardLength (asserted)
    - Empty-events and all-zero-weight boards are entirely null (asserted)
    - buildParticipants team mode produces exactly `count` tokens regardless of member counts (D-01 shared-token, asserted)
    - canStart([]) is not ok and carries a non-empty reason; canStart(non-empty) is ok
    - Engine purity gate still zero: `grep -rnE "^import .* from ['\"](react|react-dom)['\"]" src/engine`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/engine/placement.test.ts src/engine/setup.test.ts</automated>
  </verify>
  <done>Board events are placed by weight with all edge cases safe; presets, participant builder (solo + shared-team-token), and the empty-mission guard are implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Turn FSM engine — full loop, overshoot win, timeout/tie, team rotation (D-03..D-08)</name>
  <files>src/engine/engine.ts, src/engine/engine.test.ts</files>
  <read_first>
    - src/engine/types.ts, src/engine/rng.ts, src/engine/placement.ts, src/engine/setup.ts (Tasks 1-2 — the pieces this FSM composes)
    - src/schema.ts (Effect values forward/backward/extra; Event.steps; Mission fields for the drawn card)
    - .planning/phases/02-playable-core-loop/02-RESEARCH.md §Pattern 1 (immutable transitions), §Pattern 3 (moveAndResolve overshoot), §Code Examples (endGame leaders), §Pitfall 1 (no wall-clock), §Pitfall 2 (tie), §Pitfall 5 (extra-turn advance)
    - .planning/phases/02-playable-core-loop/02-CONTEXT.md §decisions D-03/D-04/D-05/D-06 (win rules, session end)
  </read_first>
  <behavior>
    - createGame → GameState phase 'awaitingDraw', boardEvents from placeEvents, currentIndex 0, winners []
    - drawCard → phase 'awaitingJudgement', card.mission set via uniformPick over missions (LOOP-01)
    - judge(state,false) → no position change, advanceTurn to next participant, phase 'awaitingDraw' (LOOP-04)
    - judge(state,true) → phase 'awaitingRoll' (LOOP-05)
    - rollDice: position += roll; position >= boardLength → phase 'gameOver', winners=[current.id], endReason 'reached-finish' (D-03 overshoot, NO exact landing required, NO upper clamp)
    - rollDice landing on an event square applies it: forward adds steps (may itself cross finish → win), backward subtracts steps clamped at 0 (never negative), extra sets LandingResult.extraTurn=true (LOOP-07)
    - advanceTurn after extraTurn keeps the SAME participant and (team) the SAME member (A4 confirmed: extra = same member repeats); otherwise moves to next participant and rotates that team's memberTurnIndex mod memberNames.length (LOOP-08, D-01, Pitfall 5)
    - endGame(state,'timeout'|'manual') → winners = participants at max position; a tie yields co-winners (length>1) per D-05; phase 'gameOver' (D-04)
    - engine.ts is clock-free — it reads no wall-clock time and schedules no timers or intervals (Pitfall 1)
    - a seeded full solo game drives awaitingDraw→…→gameOver with a single winner; a seeded team game shares one token and rotates members
  </behavior>
  <action>
    Create src/engine/engine.ts as pure immutable transitions `(state, ...args, rng) => GameState` (RESEARCH Pattern 1 — mirror the Phase 1 store's `set((s)=>({...}))` immutable style; return new objects, never mutate).

    Implement: `createGame(config, missions, events, rng)` (boardEvents via placeEvents, phase 'awaitingDraw'); `drawCard(state, missions, rng)` (uniformPick a mission → phase 'awaitingJudgement', LOOP-01); `judge(state, success)` (false → advanceTurn, LOOP-04; true → 'awaitingRoll', LOOP-05); `rollDice(state, events, rng)` composing an internal moveAndResolve per RESEARCH Pattern 3 — `from + roll`, immediate win when `>= boardLength` BEFORE event application (D-03 overshoot, no upper clamp), else look up boardEvents[landing], apply forward(+steps, may re-check win)/backward(max(0, -steps))/extra(extraTurn=true, steps===0 guaranteed by schema refine), producing a LandingResult and phase 'turnResolved' or 'gameOver' (LOOP-07/09); `advanceTurn(state)` honoring lastLanding.extraTurn (same participant + same member per A4) vs next participant with team memberTurnIndex rotation (LOOP-08, D-01, Pitfall 5), phase back to 'awaitingDraw'; `endGame(state, reason)` computing max position and returning ALL participants at that max as winners (co-winners on tie, D-04/D-05, Pitfall 2); `resetGame(state, missions, events, rng)` = fresh createGame with the same config (supports LOOP-10 restart).

    engine.ts must stay clock-free — it reads no wall-clock time and schedules no timers or intervals; the countdown is DOM-owned (D-04, Pitfall 1) — and must not import React/DOM or src/store.ts (D-09); missions and events arrive only as transition arguments.

    Create engine.test.ts exhaustively exercising the behavior block with fixed mulberry32 seeds: fail→no-move→next, success→roll→move, overshoot win (single winner), forward-event-crosses-finish win, backward clamp at 0, extra→same participant repeats, endGame timeout tie→co-winners, endGame manual→furthest wins, team shared-token + member rotation, and one full solo game to victory.
  </action>
  <acceptance_criteria>
    - `npm test -- src/engine/engine.test.ts` passes (every behavior-block case green)
    - `npm test` (full suite) passes — Phase 1 tests still green, engine suites added
    - Overshoot win asserted: a roll landing beyond boardLength wins without exact landing (D-03)
    - Timeout tie asserted: two participants at equal max position both appear in winners (D-05)
    - No wall-clock in engine (comment lines filtered out first): `grep -vE '^\s*(//|\*|/\*)' src/engine/engine.ts | grep -nE "Date\.now|setInterval|performance\.now"` returns zero lines (Pitfall 1)
    - Engine purity gate still zero: `grep -rnE "^import .* from ['\"](react|react-dom)['\"]" src/engine`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/engine/engine.test.ts</automated>
  </verify>
  <done>The complete turn FSM plays a full deterministic game to a win, honors overshoot + timeout/tie + team-token rules, and stays pure — the durable engine contract for the harness (02-02/02-03) and Phase 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| setup form → engine | Participant count / names cross from human input into buildParticipants; only untrusted surface, and it is a single local operator (강사) |
| Phase 1 content → engine | Missions/events injected into transitions; already Zod-validated at Phase 1 import (src/lib/io.ts ContentSchema.safeParse) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Denial of Service | buildParticipants(count) | low | mitigate | Clamp count into [1, MAX_PARTICIPANTS=8]; placement loops are bounded by boardLength≤30 |
| T-02-02 | Tampering | injected missions/events | low | accept | Data is validated by Phase 1 ContentSchema on import; engine consumes validated types only, no new external input |
| T-02-03 | Information Disclosure | RNG predictability | low | accept | Deterministic RNG is a testing feature, not a security control; a classroom tool has no cheating threat model (RESEARCH §Security Domain) |
| T-02-04 | Denial of Service | weightedPick float edge | low | mitigate | weightedPick returns null on total≤0 and has a final-item float-safety fallback (no infinite loop / NaN) |

No new dependencies are installed this phase (RESEARCH §Package Legitimacy Audit: none), so no supply-chain (T-02-SC) checkpoint applies.
</threat_model>

<verification>
- `npm test` — full vitest suite green (four new engine suites + all Phase 1 suites)
- Engine purity: `grep -rnE "^import .* from ['\"](react|react-dom)['\"]" src/engine` returns nothing
- No wall-clock in engine (comment lines filtered first): `grep -vE '^\s*(//|\*|/\*)' src/engine/engine.ts | grep -nE "Date\.now|setInterval|performance\.now"` returns nothing (D-04/Pitfall 1)
- Distribution tests present and passing (dice fairness, weighted placement ratio) per D-07
</verification>

<success_criteria>
- The pure engine (types/rng/placement/setup/engine) exists under src/engine with no React/DOM/store imports
- A seeded full game plays start→win in a unit test (D-03 overshoot) and endGame yields co-winners on a tie (D-05)
- Team mode shares one token with member rotation (D-01); empty mission library is blocked by canStart (MISSION-07)
- Weighted event placement and dice fairness are proven by seeded distribution tests (EVENT-06, D-07)
</success_criteria>

<output>
Create `.planning/phases/02-playable-core-loop/02-01-SUMMARY.md` when done. Record the final engine public API signatures (they are the Phase 3 contract), the confirmed Claude-discretion values (short=20 / normal=30 squares, density=0.5, default count=2, MAX_PARTICIPANTS=8, extra-turn keeps same team member), and any deviations.
</output>
