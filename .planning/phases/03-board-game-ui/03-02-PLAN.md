---
phase: 03-board-game-ui
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/game/styles/game.css
  - src/game/hud/TurnHud.tsx
  - src/game/hud/MissionOverlay.tsx
  - src/game/hud/ControlsBar.tsx
  - src/game/hud/DiceResultPanel.tsx
  - src/game/hud/PositionReadout.tsx
  - src/game/hud/hud.test.tsx
  - src/game/hud/SetupView.tsx
  - src/game/hud/ResultView.tsx
  - src/game/hud/shell.test.tsx
  - src/game/PlayView.tsx
  - src/game/GameApp.tsx
  - src/main.tsx
  - src/App.test.tsx
  - src/harness/GameHarness.tsx
  - src/harness/PlayHarness.tsx
  - src/harness/PlayHarness.test.tsx
  - src/harness/SetupScreen.tsx
  - src/harness/SetupScreen.test.tsx
  - src/harness/ResultScreen.tsx
  - src/harness/ResultScreen.test.tsx
autonomous: true
requirements: [ART-04]
user_setup: []

must_haves:
  truths:
    - "The whole game flow (start → play → result) is operated with big, simple, minimal-text buttons — every primary action button is ≥72px tall (ART-04, D-09)."
    - "A mission card overlay shows name/desc/difficulty large; 성공/실패 judge it; a dice-result panel shows the roll + move + event banner; the result screen resolves a tie (D-06/D-09)."
    - "The Phase 1 instructor editor stays reachable via the 게임/편집기 mode switch, with 게임 as the default entry (D-10; the default flip is implemented in 03-01, this plan does not modify App.tsx)."
    - "Consecutive games run with no GPU-memory growth on the target tablet (D-08, Success Criterion 4) — confirmed by the end-of-phase perf/leak check."
  artifacts:
    - "src/game/styles/game.css — --tap/--tap-sm/--hud-h + 4-size game type scale extending Phase 1 index.css"
    - "src/game/hud/{TurnHud,MissionOverlay,ControlsBar,DiceResultPanel,PositionReadout}.tsx — child play HUD (ART-04)"
    - "src/game/hud/{SetupView,ResultView}.tsx — child-reskinned setup/result reusing Phase 2 logic"
    - "src/game/GameApp.tsx — routes setup/play/result on the reskinned views"
  key_links:
    - "ControlsBar hides/disables while usePresentation.busy is true; re-enables on ANIM_DONE (D-07)."
    - "game.css tokens extend src/styles/index.css verbatim; game scale (40/26/20/15) scoped to the game screen only."
    - "GameApp routes on game phase; App.tsx already renders GameApp for the 게임 view AND defaults the entry to 게임 (both from 03-01) — App.tsx is NOT modified here (D-10)."
    - "Throwaway harness screens are deleted; the reusable useGameStore bridge is kept."
---

<objective>
Replace the throwaway Phase 2 plain-DOM harness with the **real child-facing game UI** (ART-04, D-09): a full-viewport DOM HUD overlaying the 03-01 R3F board — big minimal-text buttons, a mission-card overlay, a current-turn HUD, a dice-result/event panel, and child-reskinned setup + result screens — all built from the approved 03-UI-SPEC tokens (extending Phase 1's bright system). The instructor editor stays reachable via the mode switch with 게임 as default (D-10). The phase closes with a target-tablet perf/leak check proving no GPU-memory growth across consecutive games (D-08, Success Criterion 4).

Purpose: Deliver ART-04 (Success Criterion 3 — the whole flow driven by big simple buttons) and retire the disposable harness, leaving the codebase on the real game UI without touching the pure engine or the `useGameStore` bridge.

Output: `src/game/hud/*` + `src/game/styles/game.css`, the reskinned `SetupView`/`ResultView`, a `GameApp` that routes to them, the deleted harness screens, and updated `App.test.tsx`. After this plan the game is fully playable and controllable via the child UI on a tablet.
</objective>

## Phase Goal

**As a** 강사가 진행하는 수업의 아동 플레이어, **I want to** 크고 단순한 버튼과 미션 카드 화면으로 시작→플레이→결과 전체를 쉽게 조작하고, **so that** 3D 보드 게임을 어른 도움 없이도 직관적으로 즐길 수 있다.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-board-game-ui/03-CONTEXT.md
@.planning/phases/03-board-game-ui/03-UI-SPEC.md
@.planning/phases/03-board-game-ui/03-RESEARCH.md

# Reuse targets (logic to re-skin, then delete the throwaway screens)
@src/harness/PlayHarness.tsx
@src/harness/SetupScreen.tsx
@src/harness/ResultScreen.tsx
@src/harness/GameHarness.tsx
@src/harness/useGameStore.ts
@src/components/SegmentedControl.tsx
@src/styles/index.css
@src/App.tsx
@.claude/CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (03-02)

| Artifact | Kind | Purpose |
|----------|------|---------|
| `src/game/styles/game.css` | CSS tokens | `--tap:72px`, `--tap-sm:56px`, `--hud-h:88px`, game 4-size scale (40/26/20/15) extending index.css (UI-SPEC Spacing/Typography) |
| `src/game/hud/TurnHud.tsx` | React | fixed top bar: current player pill + team sub-line + timer + 지금 순위로 마치기, `aria-live` (ART-04) |
| `src/game/hud/MissionOverlay.tsx` | React | centered mission card (name/desc/diff, big) + 성공/실패 pair (D-06/LOOP-02/03) |
| `src/game/hud/ControlsBar.tsx` | React | phase-driven big buttons (카드 뽑기 / 주사위 굴리기 / 다음), disabled while busy (D-07/ART-04) |
| `src/game/hud/DiceResultPanel.tsx` | React | dice value + move readout + event banner (forward/back/extra) (LOOP-05/07/08) |
| `src/game/hud/PositionReadout.tsx` | React | every token's name — N칸, current marked |
| `src/game/hud/SetupView.tsx` | React | child re-skin of SetupScreen (reuse ALL Phase 2 logic, empty-library guard) |
| `src/game/hud/ResultView.tsx` | React | child re-skin of ResultScreen (winner/tie radio, 다시 시작/시작 화면으로) |
| `src/game/GameApp.tsx` (updated) | React | routes setup(SetupView)/play(PlayView+HUD)/result(ResultView) |
| DELETED: `src/harness/{GameHarness,PlayHarness,SetupScreen,ResultScreen}.tsx` (+ their tests) | — | throwaway harness retired |
| KEPT: `src/harness/useGameStore.ts` (+ test) | — | reusable engine⇄content bridge, unchanged (CONTEXT reuse seam) |
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Game-screen tokens + child play HUD overlay (big buttons, mission card, turn HUD) wired into PlayView</name>
  <files>src/game/styles/game.css, src/game/hud/TurnHud.tsx, src/game/hud/MissionOverlay.tsx, src/game/hud/ControlsBar.tsx, src/game/hud/DiceResultPanel.tsx, src/game/hud/PositionReadout.tsx, src/game/hud/hud.test.tsx, src/game/PlayView.tsx, src/main.tsx</files>
  <read_first>
    - .planning/phases/03-board-game-ui/03-UI-SPEC.md — Spacing Scale (--tap 72 / --tap-sm 56 / --hud-h 88), Typography (game 4-size 40/26/20/15, weights 500/800/900), Color (accent reserved list, semantic grass/coral/grape/sun), Component Inventory (Turn HUD, Mission card overlay, 성공/실패, Dice control, Dice result + move panel, Event effect banner, Position readout, Big game button), Copywriting Contract (exact Korean strings + emoji), Interaction & Motion (FSM phase→controls, busy gating), Accessibility (tap targets, contrast, color independence, focus, live regions)
    - src/harness/PlayHarness.tsx — the exact loop flow + strings + DOM-owned countdown pattern to re-skin (draw/judge/roll/next, DIFF_LABEL, formatClock, end('manual'))
    - src/game/PlayView.tsx (from 03-01) — the stage that hosts BoardScene; replace its thin controls with these HUD components
    - src/game/usePresentation.ts (from 03-01) — busy flag to gate controls
    - src/harness/useGameStore.ts — bridge actions to call (unchanged)
    - src/styles/index.css — inherited tokens + press affordance (bottom-shadow collapse + translateY(2px)) to extend, NOT fork
    - src/main.tsx — where index.css is imported (add game.css import alongside)
  </read_first>
  <action>
    Create `src/game/styles/game.css` importing/extending the Phase 1 tokens (do NOT invent new hues, radii, or weights — UI-SPEC "EXTENDS Phase 1"): declare the additive named tokens `--tap:72px`, `--tap-sm:56px`, `--hud-h:88px`, and the game-screen 4-size type scale (Display 40/900, Heading 26/900, Body 20, Caption 15) as CSS custom properties + utility classes. Reuse the inherited press affordance (bottom-shadow collapse + `translateY(2px)` on `:active`, ~0.08s) scaled up to `--tap`. Add `@media (prefers-reduced-motion: reduce)` handling consistent with index.css. Ensure the play stage is a full-viewport layout (not the 1080px editor container) with `env(safe-area-inset-*)` padding. Import `game.css` from `src/main.tsx` (next to the existing index.css import).

    Build the DOM HUD components as siblings of the `<Canvas>` (Pattern 6 — real `<button>`/`<input>`, NOT drei `<Html>`), each using the tokens above and the exact Copywriting Contract strings:
    - `TurnHud.tsx`: fixed `--hud-h` top bar (`--panel`, `--radius`). Left: current player name pill (`○○ 차례`, Display) with active `--sky` tint; team sub-line `이번엔 ○○ 님이 도전!` (Body) when mode==='team'. Right: `⏱️ MM:SS` timer (Caption, tabular-nums, only if timeLimitMs != null) + `지금 순위로 마치기` secondary button (`--tap-sm`) that opens the reused accessible `ConfirmDialog` (`지금 순위로 게임을 마칠까요?`) → `end('manual')`. `aria-live="polite"` announces turn changes.
    - `MissionOverlay.tsx`: centered `--panel` card (radius 24px, 2xl padding, max-width ~560px) shown at `awaitingJudgement`. Mission name (Heading 26/900), description (Body, `--ink-soft`), difficulty badge (Caption pill; easy=grass / normal=sun / hard=coral using DIFF_LABEL 쉬움/보통/어려움). Below: the 성공/실패 button pair — `✅ 성공` (`--grass`) and `❌ 실패` (`--coral`), both `--tap`, `#fff` text, each carrying a text label (color independence).
    - `ControlsBar.tsx`: the phase-driven primary buttons per the UI-SPEC FSM table — `🎴 카드 뽑기` (awaitingDraw), `🎲 주사위 굴리기` (awaitingRoll), `다음 ➡️` (turnResolved, neutral `--panel-2`). All `--tap`, sky gradient for progression buttons. Every control is hidden/disabled while `usePresentation.busy` is true and re-enabled only on ANIM_DONE (D-07) — idempotent, so a double-tap cannot skip a phase.
    - `DiceResultPanel.tsx`: shown at `turnResolved`. Dice value (`🎲 N`, Display, tabular-nums), move readout `○칸 → ○칸` (Body, tabular-nums), and the event effect banner when `lastLanding.eff`: forward=`➡️ N칸 앞으로!` (grass), backward=`⬅️ N칸 뒤로!` (coral), extra=`🔁 한 번 더!` (grape) with optional 🎁 보너스 / 🕳️ 함정 chip — text + emoji + color, never color-only.
    - `PositionReadout.tsx`: compact list of every participant `name — N칸` (tabular-nums), current player marked; reuse engine participants[].position.

    Integrate all of these into `PlayView.tsx` as the absolute DOM overlay over `<BoardScene>` (replacing the 03-01 thin controls), keeping the DOM-owned countdown effect reused verbatim from `PlayHarness` (clears on unmount — no timer leak). Announce the dice result via `aria-live="polite"` and the timeout end via `assertive` (Accessibility). All mission/event text renders through JSX (auto-escaped — no `dangerouslySetInnerHTML`).

    jsdom tests in `hud.test.tsx` (RTL, mirror the harness test style — the `<Canvas>` is not mounted here; test the HUD components/PlayView-with-mocked-scene in isolation): assert phase→visible-controls mapping (awaitingDraw shows 카드 뽑기, awaitingJudgement shows mission + 성공/실패, awaitingRoll shows 주사위 굴리기, turnResolved shows dice result + 다음), that controls are disabled/absent when busy is true, difficulty badge label mapping, event-banner copy per effect, and that the turn HUD has an `aria-live` region. Assert primary buttons carry the `--tap` sizing class/attribute (ART-04).
  </action>
  <verify>
    <automated>npm test -- src/game/hud/hud.test.tsx</automated>
    <automated>npm run build</automated>
    <human-check>On the tablet in 게임 mode: the play screen shows big (≥72px) minimal-text buttons, a top turn HUD, and the mission card overlay; while an animation plays the buttons are hidden/disabled and reappear only after it finishes; 성공/실패 and 다음 are easy for a child to tap without mis-hits.</human-check>
  </verify>
  <acceptance_criteria>
    - `game.css` defines `--tap:72px`, `--tap-sm:56px`, `--hud-h:88px` and the 4-size game scale, extending (not forking) index.css tokens; imported via main.tsx.
    - Every PRIMARY game button is ≥72px tall and carries a text label + emoji (color independence) — asserted in hud.test.tsx and human-check (ART-04).
    - hud.test.tsx passes: phase→controls table, disabled-while-busy (D-07), difficulty/event copy, aria-live present.
    - `npm run build` passes.
    - No `dangerouslySetInnerHTML`; all content is React-escaped text (T-03-01).
  </acceptance_criteria>
  <done>The in-play screen is driven by big, simple, minimal-text child controls over the 3D board, gated by ANIM_DONE, with the mission overlay, turn HUD, dice-result/event panel, and position readout wired to the engine.</done>
</task>

<task type="auto">
  <name>Task 2: Child-reskin setup + result, route via GameApp, delete throwaway harness, tablet perf/leak check</name>
  <files>src/game/hud/SetupView.tsx, src/game/hud/ResultView.tsx, src/game/hud/shell.test.tsx, src/game/GameApp.tsx, src/App.test.tsx, src/harness/GameHarness.tsx, src/harness/PlayHarness.tsx, src/harness/PlayHarness.test.tsx, src/harness/SetupScreen.tsx, src/harness/SetupScreen.test.tsx, src/harness/ResultScreen.tsx, src/harness/ResultScreen.test.tsx</files>
  <read_first>
    - .planning/phases/03-board-game-ui/03-UI-SPEC.md — Component Inventory (Setup screen re-skin, Result screen, Mode switch), Copywriting Contract (setup logo/subtitle/start, result title/reason/winner/tie strings, mode switch 🎮 게임 / ✏️ 편집기), Accessibility (focus-trapped tie radio + confirm), Handoff Notes (reuse logic, delete harness)
    - src/harness/SetupScreen.tsx — ALL setup logic to reuse (mode/count/names/characters/members/preset/timeLimit, canStart empty-library guard, buildParticipants/boardLengthFor/DEFAULT_TIME_LIMIT_MS)
    - src/harness/ResultScreen.tsx — REASON_COPY + tie-break radio logic to reuse
    - src/harness/GameHarness.tsx — the routing being replaced by GameApp
    - src/game/GameApp.tsx (from 03-01) — currently reuses harness Setup/Result; repoint to the new views
    - src/App.tsx — already renders GameApp for the 게임 view AND already defaults the entry to 게임 (both from 03-01); do NOT re-add GameHarness and do NOT modify App.tsx here
    - src/App.test.tsx — App tests from 03-01 (editor tests click 편집기 first; a game-default first-mount assertion already exists); here only repoint the game-view assertions at the reskinned SetupView after harness deletion — keep the 03-01 game-default assertion intact
    - src/components/SegmentedControl.tsx, src/components/ConfirmDialog.tsx, src/components/Modal.tsx — reused accessible primitives
    - .planning/phases/03-board-game-ui/03-RESEARCH.md — Pitfall 2 (GPU leak check via gl.info.memory across 5+ games), Don't Hand-Roll (R3F auto-dispose), Environment Availability (tablet perf is a manual on-device check)
  </read_first>
  <action>
    Create `src/game/hud/SetupView.tsx` re-skinning `src/harness/SetupScreen.tsx`: reuse ALL its logic verbatim (mode, count steppers, names, characters, team members, board preset, time-limit toggle, `canStart` empty-library guard, `buildParticipants`/`boardLengthFor`/`startGame`). Restyle child-friendly with `--tap`/`--tap-sm` controls and the reused `SegmentedControl`; placeholder text logo `파워점핑` + subtitle `신나는 줄넘기 미션 — 게임을 설정하고 시작하세요.` (real logo = Phase 4/ART-05). 시작 button = `--tap` primary, disabled with the engine `canStart` reason copy when the mission library is empty (MISSION-07). No new game rules — Phase 2 logic only.

    Create `src/game/hud/ResultView.tsx` re-skinning `src/harness/ResultScreen.tsx`: reuse `REASON_COPY` and the tie-break radio logic. Title `게임 끝!` (Display), reason line (Heading), winner `🏆 승리: ○○` (Display); tie → `공동 승리! 최고 칸에 함께 도착했어요.` + focus-managed radio group `최종 승자를 선택하세요` styled as `--tap-sm` options → `🏆 최종 승리: ○○`. Final standings list. Actions: `🔁 다시 시작` (`--tap` primary sky → `startGame(config)`) + `시작 화면으로` (`--tap-sm` secondary → `reset()`).

    Update `src/game/GameApp.tsx` to route to the new views: `game === null` → `SetupView`; `game.phase === 'gameOver'` → `ResultView`; otherwise → `PlayView`. (App.tsx already mounts GameApp for the 게임 view from 03-01 — the 게임/편집기 mode switch with 게임-default entry stays, D-10.)

    Delete the throwaway harness screens and their tests: `src/harness/GameHarness.tsx`, `PlayHarness.tsx`, `PlayHarness.test.tsx`, `SetupScreen.tsx`, `SetupScreen.test.tsx`, `ResultScreen.tsx`, `ResultScreen.test.tsx`. KEEP `src/harness/useGameStore.ts` + `useGameStore.test.ts` (the reusable engine⇄content bridge — CONTEXT reuse seam; unchanged). Ensure no remaining import references the deleted files (grep for `harness/GameHarness`, `harness/PlayHarness`, `harness/SetupScreen`, `harness/ResultScreen`).

    Update `src/App.test.tsx` for the harness deletion: repoint the game-view assertions at the reskinned GameApp/SetupView (the deleted harness screens no longer render). Do NOT re-flip or re-implement the default — the 'editor'→'game' default flip and its first-mount assertion already landed in 03-01, and `src/App.tsx` is NOT modified in this plan. This task only keeps the game-view test green against the new SetupView and confirms 편집기 stays reachable via the mode switch; leave the 03-01 game-default assertion intact.

    Write `src/game/hud/shell.test.tsx` (jsdom/RTL): SetupView blocks 시작 with guidance when the mission library is empty and enables it when non-empty (reuse the harness test assertions); ResultView renders a single winner and, for a tie, the radio group that resolves to a final winner; 다시 시작 calls startGame and 시작 화면으로 calls reset.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>npm run build</automated>
    <human-check>TABLET PERF/LEAK (Success Criterion 4, D-08): on the target tablet, log `gl.info.memory` (geometries/textures) and `gl.info.render.calls` after each game; play 5+ consecutive games (다시 시작 and 시작 화면으로 → new game) and confirm the counts return to a flat baseline (no monotonic growth) and frame pacing stays smooth. Verify reduced-motion still advances the game. Confirm 편집기 mode still opens the Phase 1 editor and 게임 is the default entry (D-10).</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test` fully green with the harness screen tests removed and new shell.test.tsx + updated App.test.tsx passing.
    - `npm run build` passes; no dangling imports to the deleted harness screens (grep clean).
    - SetupView reuses Phase 2 setup logic (empty-library guard blocks 시작 with the engine reason); ResultView reuses REASON_COPY + tie-break (D-05).
    - GameApp routes setup/play/result on the reskinned views; the 게임-default entry (flipped in 03-01) and 편집기 reachability remain intact — App.test renders the reskinned game view on first mount and reaches the editor via the switch (D-10). `src/App.tsx` is NOT modified in this plan.
    - `src/harness/useGameStore.ts` (+ test) retained and unchanged; the four harness screen files + their tests are deleted.
    - MANUAL: GPU memory flat across 5+ consecutive games (no leak, D-08 / Success Criterion 4); reduced-motion playable.
  </acceptance_criteria>
  <done>The throwaway harness is retired; setup/play/result run entirely on the child-friendly UI over the 3D board, the editor coexists (게임 default), and the target tablet shows stable GPU memory across consecutive games.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported content → DOM HUD | Mission/event text (zod-validated at Phase 1 import) rendered in the mission overlay, dice-result/event banners, and setup. |
| user → live game control | Big-button actions drive engine transitions; a disruptive 마치기 ends a live session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering/Info | mission/event text in MissionOverlay + DiceResultPanel + SetupView | low | mitigate | React text nodes only (auto-escaped); no `dangerouslySetInnerHTML`; content already zod-validated at Phase 1 import. No new import surface. |
| T-03-04 | Tampering (UX) | accidental 지금 순위로 마치기 ending a live game | low | mitigate | Gate behind the reused accessible `ConfirmDialog` (focus-trapped, Esc-cancel) before `end('manual')` — a stray tap can't end the game. |
| T-03-05 | DoS (input) | oversized participant/board config | low | accept | Bounded by engine MAX_PARTICIPANTS (8) + board presets reused from Phase 2; no new limits needed. |

Client-only, offline, no-network, no-auth (ASVS L1). No new secrets/sessions/network calls (RESEARCH Security Domain).
</threat_model>

<verification>
- `npm test` green: hud.test.tsx, shell.test.tsx, updated App.test.tsx; harness screen tests removed; useGameStore.test.ts retained.
- `npm run build` passes; grep confirms no imports of deleted harness screens.
- Automated: phase→controls, disabled-while-busy (D-07), setup empty-library guard, result tie-break, mode-switch default.
- MANUAL/UAT (end-of-phase human_verify_mode): tablet perf/leak (gl.info.memory flat across 5+ games, D-08/Success Criterion 4), big-button child ergonomics (ART-04), reduced-motion playability, editor coexistence (D-10). This is the phase-closing human verification.
</verification>

<success_criteria>
- ART-04 satisfied: the entire start→play→result flow is driven by big (≥72px), minimal-text buttons (Success Criterion 3).
- D-09/D-10 satisfied: the child UI replaces the harness; the editor stays reachable with 게임 as default entry.
- D-08 / Success Criterion 4 confirmed: no GPU-memory growth across 5+ consecutive games on the target tablet.
- The engine (src/engine/*) and the useGameStore bridge remain unchanged; only the throwaway harness screens are deleted.
</success_criteria>

<output>
Create `.planning/phases/03-board-game-ui/03-02-SUMMARY.md` when done.
</output>
