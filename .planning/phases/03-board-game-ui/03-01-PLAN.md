---
phase: 03-board-game-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/game/boardLayout.ts
  - src/game/boardLayout.test.ts
  - src/game/diceRotation.ts
  - src/game/diceRotation.test.ts
  - src/game/usePresentation.ts
  - src/game/usePresentation.test.ts
  - src/game/scene/BoardScene.tsx
  - src/game/scene/BoardTiles.tsx
  - src/game/scene/Token.tsx
  - src/game/scene/Dice.tsx
  - src/game/scene/scene.test.tsx
  - src/game/PlayView.tsx
  - src/game/GameApp.tsx
  - src/App.tsx
  - src/App.test.tsx
autonomous: true
requirements: [LOOP-06]
user_setup: []

must_haves:
  truths:
    - "Rolling the dice moves the current player's token diceValue squares along the 3D board with a visible hop animation (LOOP-06, D-03/D-04)."
    - "The next-phase controls stay hidden/disabled until the running 3D animation fires ANIM_DONE — no double-tap can skip the engine forward (D-07)."
    - "A full game (start → play → result) is playable end-to-end with the 3D board and moving token visible (Success Criterion 1)."
    - "The dice mesh spins then snaps to the engine's pre-rolled 1-6 face; no physics engine is installed (D-05, D-02)."
    - "The app opens in 게임 mode by default — App.tsx initializes the view state to 'game' (not 'editor') — while the 편집기 editor stays reachable via the mode switch (D-10)."
  artifacts:
    - "src/game/boardLayout.ts — pure squareIndex → THREE.Vector3 mapping (D-03)"
    - "src/game/diceRotation.ts — pure face(1-6) → target Euler mapping (D-05)"
    - "src/game/usePresentation.ts — busy flag + signalAnimDone + watchdog (D-07)"
    - "src/game/scene/{BoardScene,BoardTiles,Token,Dice}.tsx — R3F scene (D-01/D-03/D-04/D-05)"
    - "src/game/PlayView.tsx + GameApp.tsx — playable 3D slice wired to useGameStore"
  key_links:
    - "usePresentation.busy gates control visibility; Token onArrive → signalAnimDone clears it (ANIM_DONE, D-07)."
    - "Token reads the engine's participant.position (via useGameStore) and lerps its visual position toward it (LOOP-06)."
    - "Canvas dpr capped [1,2]; tile/token geometry+material shared and auto-disposed (D-08)."
    - "App.tsx flips the default view state 'editor'→'game' and renders GameApp (not the throwaway GameHarness) in the game branch; the mode switch keeps 편집기 reachable (D-10)."
---

<objective>
Install the React Three Fiber stack (D-01/D-02) and build a **playable 3D board slice**: the Phase 2 pure engine, unchanged, is now visualized as a low-poly 3D board where the current player's token animates `diceValue` squares on a roll (LOOP-06), the dice mesh spins and snaps to the engine's pre-rolled face (D-05), and every 3D flourish gates the next engine transition via an ANIM_DONE `busy` flag (D-07). Resource hygiene (shared geometry/material, auto-dispose, dpr cap) is built in from the first mount (D-08).

Purpose: Deliver the load-bearing Success Criterion 1 (3D token move) and the ANIM_DONE mechanism (Success Criterion 2) as a real, end-to-end-playable slice — reusing the existing `useGameStore` bridge and engine verbatim. Plan 03-02 then wraps this slice in the polished child HUD.

Output: `src/game/` module (pure layout/rotation math + presentation store + R3F scene + PlayView + GameApp), the R3F dependencies installed, and `App.tsx` routing the game view to `GameApp` **and defaulting the app's entry to 게임 mode** (D-10 — editor still reachable via the switch). After this plan, launching the app opens 게임 mode showing the 3D board, and a full game can be played with the token visibly moving.
</objective>

## Phase Goal

**As a** 줄넘기 수업을 듣는 아동, **I want to** 카드로 뽑은 미션을 성공하면 3D 보드 위에서 내 말이 주사위 수만큼 신나게 이동하는 걸 보며 게임을 처음부터 끝까지 플레이하고, **so that** Phase 2에서 검증된 게임 루프가 눈에 보이는 3D 연출로 재미있게 돌아간다.

> Note: the ROADMAP `**Goal:**` line for Phase 3 is outcome-prose, not the canonical user-story form; the story above faithfully restates that goal (it does not add scope). Surfaced in the planner return.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-board-game-ui/03-CONTEXT.md
@.planning/phases/03-board-game-ui/03-RESEARCH.md
@.planning/phases/03-board-game-ui/03-UI-SPEC.md

# Reuse contract (engine + bridge are UNCHANGED — read, do not modify engine logic)
@src/engine/types.ts
@src/engine/engine.ts
@src/engine/setup.ts
@src/harness/useGameStore.ts
@src/App.tsx
@.claude/CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (03-01)

| Artifact | Kind | Purpose |
|----------|------|---------|
| `src/game/boardLayout.ts` (+ test) | pure module | `squarePosition(index) → Vector3` snake/straight path (D-03) |
| `src/game/diceRotation.ts` (+ test) | pure module | `FACE_UP_EULER[1..6]` face-up rotation table (D-05) |
| `src/game/usePresentation.ts` (+ test) | Zustand store | `busy` flag, `beginAnim`, `signalAnimDone`, watchdog (ANIM_DONE, D-07) |
| `src/game/scene/BoardScene.tsx` | R3F component | `<Canvas>` root: fixed iso camera, dpr `[1,2]`, theme clear color, lights, `<Bounds>` (D-01/D-08) |
| `src/game/scene/BoardTiles.tsx` | R3F component | instanced path tiles + highlighted finish (D-03) |
| `src/game/scene/Token.tsx` | R3F component | placeholder pawn + drei `<Html>` name label + hop tween → ANIM_DONE (D-04/LOOP-06) |
| `src/game/scene/Dice.tsx` | R3F component | white cube + pips, spin → snap to pre-rolled face (D-05) |
| `src/game/PlayView.tsx` | React | `<BoardScene>` + thin phase controls (upgraded to full HUD in 03-02) |
| `src/game/GameApp.tsx` | React | routes setup / play / result (setup+result temporarily reuse harness screens) |
| deps: `three@0.185.1`, `@react-three/fiber@9.6.1`, `@react-three/drei@10.7.7`, dev `@types/three@0.185.1`, `@react-three/test-renderer@9.1.0` | npm | R3F stack (D-02) |

Replaced later (03-02): the thin PlayView controls and the harness setup/result screens.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install R3F stack + pure layout/rotation math + ANIM_DONE presentation store</name>
  <files>package.json, package-lock.json, src/game/boardLayout.ts, src/game/boardLayout.test.ts, src/game/diceRotation.ts, src/game/diceRotation.test.ts, src/game/usePresentation.ts, src/game/usePresentation.test.ts</files>
  <read_first>
    - .planning/phases/03-board-game-ui/03-RESEARCH.md — Standard Stack (pinned versions + Package Legitimacy Audit), Pattern 2 (boardLayout snake), Pattern 4 (busy store), Pattern 5 (FACE_UP_EULER), Pitfall 1 (ANIM_DONE deadlock + watchdog), Pitfall 5 (three/@types/three version pinning)
    - .planning/phases/03-board-game-ui/03-UI-SPEC.md — Interaction & Motion (ANIM_DONE gating, reduced-motion contract), Motion timings
    - src/engine/types.ts — GameState/Participant/LandingResult shape (position, lastRoll, lastLanding.from/to/extraTurn)
    - src/engine/setup.ts — boardLengthFor (short=20 / normal=30), MAX_PARTICIPANTS=8
    - src/harness/useGameStore.test.ts — existing vitest style to mirror (describe/it/expect, deterministic)
    - .npmrc (legacy-peer-deps=true), package.json (React 19.2.8 / Vite 8.1.5), vitest.config.ts (jsdom, globals)
  </read_first>
  <action>
    Install the exact pinned, peer-verified versions from RESEARCH.md Standard Stack: runtime `three@0.185.1`, `@react-three/fiber@9.6.1`, `@react-three/drei@10.7.7`; dev `@types/three@0.185.1`, `@react-three/test-renderer@9.1.0`. Use the project's existing npm + `.npmrc` (legacy-peer-deps). Do NOT install any physics engine (rapier/cannon) — dice is a tween (D-02/D-05). Pin `@types/three` to the same major.minor as `three` (Pitfall 5). The RESEARCH Package Legitimacy Audit already cleared these (the `too-new` SUS on three/@types/three is an approved recency false-positive; no human checkpoint required).

    Create `src/game/boardLayout.ts` (D-03, Pattern 2): export a pure `squarePosition(index: number): Vector3` that lays indices 0..boardLength on a snake (boustrophedon) path with named constants `COLS` and `GAP` (port the values from RESEARCH Pattern 2). No React import. Keep it deterministic and side-effect free so it is unit-testable. Also export a small helper describing the row/col so tests can assert the snake turn.

    Create `src/game/diceRotation.ts` (D-05, Pattern 5): export `FACE_UP_EULER: Record<1|2|3|4|5|6, Euler>` mapping each die face to the rotation that brings that face up (opposite faces sum to 7). Port the table from RESEARCH Pattern 5. Add a docstring noting the exact Euler values are visually validated against the authored cube mesh in Task 2's human-check (Open Question 2) — the table is the single source the Dice tween snaps to.

    Create `src/game/usePresentation.ts` (D-07, Pattern 4): a tiny Zustand `create` store exposing `busy: boolean`, `beginAnim(): void` (sets busy true), `signalAnimDone(): void` (sets busy false), and an idempotent design so calling `signalAnimDone` twice is safe. Include a `startWatchdog(durationMs)` helper that arms a `setTimeout` fallback which force-clears `busy` after the animation budget + 500ms and is cleared on normal completion (Pitfall 1 — the game must never lock forever). Keep wall-clock use confined to this watchdog only; the engine stays clock-free.

    Write co-located vitest tests mirroring the existing engine/harness test style:
    - boardLayout.test.ts: assert 0..boardLength produce distinct coordinates, the path is monotonic within a row, and the snake reverses direction between rows.
    - diceRotation.test.ts: assert all six faces are present and pairwise-distinct rotations, and the opposite-face pairing (1↔6, 2↔5, 3↔4) holds per the chosen table.
    - usePresentation.test.ts: assert busy is false initially, true after beginAnim, false after signalAnimDone; signalAnimDone is idempotent; the watchdog force-clears busy after its timeout (use vitest fake timers).
  </action>
  <behavior>
    - squarePosition(0) ≠ squarePosition(1) ≠ ... ≠ squarePosition(boardLength); all distinct.
    - squarePosition rows alternate travel direction (snake): the x of successive indices increases within an even row and decreases within the next (odd) row.
    - FACE_UP_EULER has keys 1..6, all six rotations distinct; opposite faces (sum 7) map to opposing orientations.
    - usePresentation: busy=false → beginAnim → busy=true → signalAnimDone → busy=false; double signalAnimDone leaves busy=false (idempotent); watchdog clears busy after budget+500ms if signalAnimDone never fires.
  </behavior>
  <verify>
    <automated>npm test -- src/game/boardLayout.test.ts src/game/diceRotation.test.ts src/game/usePresentation.test.ts</automated>
    <automated>npm ls three @react-three/fiber @react-three/drei @types/three @react-three/test-renderer</automated>
  </verify>
  <acceptance_criteria>
    - The five packages install at the pinned versions and `npm ls` reports no unmet peer errors (legacy-peer-deps active).
    - No physics engine (rapier/cannon) appears in package.json (D-02).
    - All three new vitest files pass; boardLayout/diceRotation/usePresentation behave per <behavior>.
    - `src/game/boardLayout.ts` and `src/game/diceRotation.ts` contain zero React/DOM imports (pure modules).
    - usePresentation watchdog guarantees busy cannot stay true forever (Pitfall 1).
  </acceptance_criteria>
  <done>R3F stack installed at pinned versions; pure boardLayout + diceRotation + ANIM_DONE presentation store exist and pass unit tests with a deadlock-proof watchdog.</done>
</task>

<task type="auto">
  <name>Task 2: R3F board scene (board/token/dice) + playable PlayView wired to the engine via ANIM_DONE</name>
  <files>src/game/scene/BoardScene.tsx, src/game/scene/BoardTiles.tsx, src/game/scene/Token.tsx, src/game/scene/Dice.tsx, src/game/scene/scene.test.tsx, src/game/PlayView.tsx, src/game/GameApp.tsx, src/App.tsx, src/App.test.tsx</files>
  <read_first>
    - .planning/phases/03-board-game-ui/03-RESEARCH.md — Pattern 1 (Canvas dpr/camera/theme), Pattern 3 (token hop tween + two-stage move for events + extra-turn zero move), Pattern 5 (dice spin→snap), Pattern 6 (DOM overlay vs drei Html), "Instanced board tiles" + "Wiring GameApp" code examples, Don't Hand-Roll (Bounds/Instances/Html/auto-dispose), Pitfall 2 (GPU leak), Pitfall 3 (no WebGL in jsdom → test-renderer), Pitfall 4 (frameloop)
    - .planning/phases/03-board-game-ui/03-UI-SPEC.md — Component Inventory (Board canvas, Placeholder token, Dice, Token color order sky/coral/grass/sun/grape/normal), Motion timings (card 0.5s / dice 0.8s / hop 0.18s), Color (board/dice placeholder, theme clear color), Accessibility (color independence via Html name label)
    - src/engine/engine.ts — rollDice (from/afterRoll/to, overshoot win, extraTurn); the visual move recovers afterRoll = from + lastRoll (Pattern 3)
    - src/engine/types.ts — GameState.phase FSM, participants[].position, lastRoll, lastLanding
    - src/harness/useGameStore.ts — the bridge actions (startGame/draw/judge/roll/next/end/reset) to call; DO NOT change it
    - src/harness/GameHarness.tsx, src/harness/PlayHarness.tsx, src/harness/SetupScreen.tsx, src/harness/ResultScreen.tsx — the flow to re-skin (reuse SetupScreen/ResultScreen as-is for now)
    - src/styles/index.css — theme tokens (--sky/--coral/--grass/--sun/--grape/--normal, --bg light/dark) to source scene colors from
    - src/App.tsx — the current view switch; line ~14 is `useState<View>('editor')` (editor is the default TODAY — this plan flips it to 'game' per D-10)
    - src/App.test.tsx — existing App tests render `<App/>` and assert editor content WITHOUT switching mode (they assume editor is default); when the default flips to 게임 they must first click 편집기 before asserting editor content
  </read_first>
  <action>
    Build the R3F scene under `src/game/scene/` following the RESEARCH patterns (reference them by name — do not re-derive):

    `BoardScene.tsx` (Pattern 1, D-01/D-08): a `<Canvas>` filling its container with `dpr={[1, 2]}` (tablet cap), a FIXED slightly-tilted isometric camera (no OrbitControls / no free orbit per UI-SPEC), `hemisphereLight` + `directionalLight`, and a theme-driven clear color read from the active token set (light: sky-tinted `--bg`; dark: deep navy) via `onCreated`/`gl.setClearColor`. Wrap the board content in drei `<Bounds fit clip observe margin>` so the whole `boardLength+1` path is auto-framed on mount and re-fit on board-length change. Default `frameloop="always"` for reliability (avoids the Pitfall 4 demand-freeze); leave a comment that demand + per-frame `invalidate()` is an optional battery optimization the 03-02 perf check may enable.

    `BoardTiles.tsx` (D-03, "Instanced board tiles" example): render indices 0..boardLength-1 as a single drei `<Instances>`/`<instancedMesh>` (one shared geometry + material → one draw call, leak-safe per D-08) positioned via `squarePosition`, and draw the finish tile at `squarePosition(boardLength)` separately, highlighted with `--sun` emissive + a 🏁 label. Share geometry/material via `useMemo` singletons; rely on R3F auto-dispose for declarative objects (do not `new` geometry in render).

    `Token.tsx` (D-04, Pattern 3, LOOP-06): one placeholder low-poly pawn per participant, colored by participant index from the fixed order sky→coral→grass→sun→grape→normal, cycling `index % 6` for participants 7-8 (Open Question 1 resolution — the drei `<Html>` name label guarantees color-independent identity, so a repeated hue is acceptable). Float a drei `<Html>` name label above each token (color independence, a11y). Animate the visual position in `useFrame` by mutating `meshRef.current.position` only (never setState per frame — anti-pattern) toward the engine's authoritative `participant.position`, hopping square-by-square at ~0.18s each with an ease-out arc (UI-SPEC). Choreograph event effects via the two-stage move from Pattern 3: recover `afterRoll = from + lastRoll`, animate `from → afterRoll`, then `afterRoll → to` (backward events run in reverse); an extra-turn (`from === to`) plays no move and must fire ANIM_DONE immediately. The active player's token wears a `--sky` highlight ring/glow. On the final hop's arrival call the move-complete callback that triggers `signalAnimDone()`.

    `Dice.tsx` (D-05, Pattern 5): a white cube with dark `--ink` pips, theme-agnostic. On roll, read `game.lastRoll` (1-6), spin ~0.8s ease-out with 2-3 extra full turns, then snap to `FACE_UP_EULER[lastRoll]`; fire ANIM_DONE on settle. The mesh is decorative — the DOM number is the a11y source of truth (added in 03-02). Reduced motion: under `prefers-reduced-motion: reduce`, skip the spin, set final rotation immediately, and fire ANIM_DONE at once (UI-SPEC).

    `PlayView.tsx`: compose `<BoardScene>` (with `BoardTiles`, a `Token` per `game.config.participants`, and `Dice`) inside a `position:relative` stage, plus a THIN phase-driven control overlay (plain buttons is fine here — the polished child HUD replaces it in 03-02) that reads `game.phase` from `useGameStore` and the `busy` flag from `usePresentation`. Flow per UI-SPEC FSM→controls table: awaitingDraw → 카드 뽑기 → `draw()`; awaitingJudgement → mission text + 성공/실패 → `judge()`; awaitingRoll → 주사위 굴리기 → `beginAnim()` then `roll()` then play dice+token tween → on ANIM_DONE reveal resolved panel; turnResolved → 다음 → `next()`. While `busy` is true, hide/disable the next-phase controls (D-07). Keep the DOM-owned countdown pattern from `PlayHarness` (reuse it) if a time limit is set; it clears on unmount (no leak).

    `GameApp.tsx` ("Wiring GameApp" example): route on the store — `game === null` → the existing `SetupScreen` (reuse harness `src/harness/SetupScreen` unchanged for now); `game.phase === 'gameOver'` → the existing `src/harness/ResultScreen` (reuse for now); otherwise → `PlayView`. 03-02 swaps setup/result for reskinned child views.

    Modify `src/App.tsx` (D-10), two changes: (a) in the existing `view === 'game'` branch, render `GameApp` instead of `GameHarness` (update the import accordingly); (b) FLIP the default entry — change the view-state initializer from `useState<View>('editor')` to `useState<View>('game')` so the app opens in 게임 mode. NOTE: editor is the default TODAY (line ~14) — this changes it; do not leave it as 'editor'. The 편집기/게임 mode switch markup itself is unchanged, so the editor stays reachable via the existing toggle. Do not otherwise touch the editor branch.

    Update `src/App.test.tsx` to match the flipped default: the existing editor-content tests render `<App/>` and assert editor tabs/mission names without switching mode — they now start in 게임 mode, so add a `fireEvent.click` on the `✏️ 편집기` toggle to enter the editor before those editor assertions (keep the assertions otherwise intact). Add a new test for the game-default entry: on first mount (no mode click) the 게임 view is active — the `🎮 게임` toggle carries the active state and the editor's MissionTab content (e.g. a seed mission name) is NOT rendered — proving App no longer initializes the view to 'editor' (D-10). Assert on stable App-level signals (the mode-toggle active state + absence of editor content), NOT GameApp-internal setup text, so the test survives the 03-02 setup re-skin.

    Scene structure test `scene.test.tsx` (Pitfall 3): use `@react-three/test-renderer` (NOT full `<Canvas>` in jsdom — no WebGL). If jsdom trips the renderer, add `// @vitest-environment node` at the top of this test file. Assert: tile count equals `boardLength+1` (instanced tiles + finish), one Token mesh per participant, and token meshes exist for a 2-participant config. Do not assert live tween frames (visual → human-check).
  </action>
  <verify>
    <automated>npm test -- src/game/scene/scene.test.tsx</automated>
    <automated>npm run build</automated>
    <human-check>In 게임 mode on the target tablet/Chrome: play a full game — the 3D board renders framed, on 성공 → 주사위 굴리기 the dice spins and snaps to a face, then the current token hops that many squares and only then do the next controls appear (ANIM_DONE). Confirm the dice top face matches the shown roll (validate FACE_UP_EULER, Open Q2), the finish tile is highlighted, and reaching/passing the finish ends the game.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` (tsc -b + vite build) passes with the R3F scene compiled.
    - scene.test.tsx passes: instanced-tile count = boardLength+1 (incl. finish), one Token per participant.
    - `src/App.tsx` initializes the view state to 'game' (no longer 'editor'); App.test.tsx asserts the 게임 view is active on first mount with editor content absent, and that 편집기 is reachable via the mode switch (D-10).
    - A full game is playable end-to-end in 게임 mode with the 3D board + moving token (LOOP-06); next-phase controls are hidden/disabled while `busy` and revealed only on ANIM_DONE (D-07) — verified in the human-check.
    - No `new THREE.*Geometry()`/material created inside a render body; tile/token geometry+material are shared via `useMemo`/`<Instances>` and rely on R3F auto-dispose (D-08).
    - `useFrame` mutates refs only (no setState per frame); the engine (src/engine/*) and `useGameStore` are unchanged.
    - Extra-turn events (from===to) fire ANIM_DONE immediately (no stuck state); reduced-motion snaps and still advances.
  </acceptance_criteria>
  <done>The validated loop is playable on a 3D board: dice spin→snap and token hop-move animate on a roll, gated by ANIM_DONE, framed by a fixed iso camera with dpr [1,2] and shared/auto-disposed geometry; App defaults to 게임 and routes it to GameApp (편집기 still reachable via the switch, D-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported content → 3D/DOM render | Mission/event text (already zod-validated at Phase 1 import) is read from the store and rendered in the scene labels / thin controls. |
| npm registry → build | New R3F dependencies enter the dependency tree this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering/Info | mission/event text in drei `<Html>` labels + thin controls | low | mitigate | Render only as React text nodes (auto-escaped); no `dangerouslySetInnerHTML`; text already zod-validated at Phase 1 import. No new import path added. |
| T-03-02 | DoS | oversized imported JSON → render load | low | accept | Out of Phase 3 scope; bounded by Phase 1 import validation + engine MAX_PARTICIPANTS/board presets. No new import surface here. |
| T-03-SC | Tampering | npm installs (three / @react-three/fiber / @react-three/drei / @types/three / test-renderer) | high | mitigate | RESEARCH Package Legitimacy Audit already ran: fiber/drei/test-renderer OK; three + @types/three are `too-new` recency false-positives (13.1M / 8.37M weekly downloads, canonical repos, no postinstall). Pin exact versions; no network/secrets introduced. Auditor disposition = Approved → no blocking human checkpoint. |
| T-03-BUNDLE | DoS (perf) | three adds significant bundle weight | low | accept | Single local classroom device, load-once; no physics dep, instanced meshes keep runtime cost low. 03-02 perf check re-evaluates; code-splitting the game bundle is an optional later optimization, not required. |

Client-only, offline, no-network, no-auth (ASVS L1) — no new secrets, sessions, or network calls (RESEARCH Security Domain).
</threat_model>

<verification>
- `npm test` green (existing suite + new boardLayout/diceRotation/usePresentation/scene tests).
- `npm run build` passes (R3F/three types resolve; @types/three pinned to three's major.minor).
- `npm ls` shows the five pinned R3F packages and no physics engine.
- Automated: pure-module behavior, ANIM_DONE busy/watchdog, scene structure (tile/token counts).
- MANUAL/UAT (end-of-phase human_verify_mode): 3D playthrough — dice face matches roll, token hop-moves diceValue squares, ANIM_DONE gating, finish highlight/win. Consolidated with the 03-02 tablet perf/leak check at end of phase.
</verification>

<success_criteria>
- LOOP-06 satisfied: on a successful roll the current token animates `diceValue` squares on the 3D board (human-check confirmed).
- ANIM_DONE (D-07) proven: next-phase controls appear only after the animation completes; watchdog prevents any lock (Pitfall 1).
- Engine + useGameStore unchanged (pure-engine contract preserved).
- Resource hygiene in place from first mount: dpr [1,2], shared/instanced geometry, auto-dispose (D-08 foundation; leak-stability confirmed in 03-02).
</success_criteria>

<output>
Create `.planning/phases/03-board-game-ui/03-01-SUMMARY.md` when done.
</output>
