# 03.1-01 SUMMARY — 2D renderer migration complete

## Context
Claude Code stopped after Phase 3.1 was inserted for the 3D→2D direction change. Hermes continued inline and implemented the first executable Phase 3.1 slice.

## Did
- Replaced R3F/Three board rendering with a DOM/CSS 2D board in `src/game/scene/BoardScene.tsx`.
- Added renderer-neutral animation contracts in `src/game/animation.ts`.
- Converted `src/game/boardLayout.ts` from Three `Vector3` world coordinates to pure 2D grid coordinates.
- Preserved `usePresentation` busy/watchdog ANIM_DONE flow from PlayView.
- Removed retired 3D scene files:
  - `src/game/scene/Token.tsx`
  - `src/game/scene/Dice.tsx`
  - `src/game/scene/MoveHighlight.tsx`
  - `src/game/scene/BoardTiles.tsx`
  - `src/game/diceRotation.ts`
- Removed Three/R3F dependencies from `package.json` and refreshed `package-lock.json`.
- Added simple CC0 SVG character sprites under `public/assets/cc0/` with license notice.
- Updated tests from R3F/WebGL assertions to DOM 2D-board assertions.
- Updated `PositionReadout` to show rank and remaining distance to finish.
- Fixed visual overlap found during browser smoke by moving dice to a floating panel and constraining board row height.

## Verification
- `npm run build` — passed.
- `npm test` — passed: 23 test files, 178 tests.
- Browser smoke — passed:
  - 2D board appears with START/FINISH, separated tiles, direction arrows, finish landmark, character tokens, and ranking.
  - Card draw → success → dice roll advanced Player 1 from 0칸 to 1칸 and updated remaining distance.

## Honest notes
- The current sprites are simple project-created CC0 SVGs, not yet downloaded Kenney Toon Characters assets. Kenney asset ingestion remains a follow-up.
- Human visual approval was not claimed; only automated/browser-smoke verification was completed.

## Follow-up
- Ingest real Kenney CC0 Toon Characters / board assets if desired.
- Polish responsive board layout on actual target tablet/phone sizes.
- Consider adding progress bars for LOOP-12 beyond the compact rank/remaining-distance strip.
